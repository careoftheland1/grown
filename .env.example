# Server-side only — do not expose this key in frontend JavaScript.
STRIPE_SECRET_KEY=sk_test_replace_me
# Map approved server-side variants to Stripe Price IDs when checkout is implemented.
STRIPE_PRICE_ENDIVE=price_replace_me
